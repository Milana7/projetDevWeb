{% extends 'base.html.twig' %}
{% block title %}Liste des sorties{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Liste des sorties </h1>

        <div id="tableSorties">
            <table border="1" >
                <tr>
                    <td height="100px">Nom de la sortie</td>
                    <td>Date de la sortie</td>
                    <td>Clôture</td>
                    <td>inscrits/places</td>
                    <td>Etat</td>
                    <td>Inscrit</td>
                    <td>Organisateur</td>
                </tr>
                {% for sortie in listSortie %}
                    <tr>
                        <td>{{ sortie.nom }} </td>
                        <td>{{ sortie.dateHeureDebut }} </td>
                        <td>{{ sortie.dateLimiteInscription }} </td>
                        <td>nbPlaces</td>
                        <td>{{ sortie.etat.libelle }}</td> {#   {{ sortie.organisateur.nom }}</td>#}
                        <td> O/N</td>
                        <td> {{ sortie.organisateur.nom }}</td>
                    </tr>
                {% endfor %}
            </table>
        </div>


    </div>
{% endblock %}